# Build performance
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8 --enable-native-access=ALL-UNNAMED
org.gradle.parallel=true
org.gradle.caching=true
# Configuration cache: off — :core:generateLibraryDescriptors and :dist:verifyPluginJar
# capture Gradle script object references (not CC-compatible yet).
# org.gradle.configuration-cache=true

systemProp.file.encoding=UTF-8

# Project coordinates
group=com.monkey.ktplus
version=4.0.0
